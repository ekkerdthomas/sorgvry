name: sorgvry
description: Sorgvry — Afrikaans health companion app.
publish_to: none
version: 0.1.0+1

resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  sorgvry_shared:
    version: any
  flutter_riverpod: ^3.0.3
  go_router: ^16.2.0
  drift: ^2.31.0
  drift_flutter: ^0.2.0
  sqlite3_flutter_libs: ^0.5.40
  http: ^1.3.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.15
  drift_dev: ^2.31.0

flutter:
  uses-material-design: true
